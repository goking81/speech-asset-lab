# F3-01｜资格候选池与本地日计划

## 目标

根据已确认个人资产与本地 S0—S5 规则生成可解释、确定性的今日训练计划；没有资产或不具资格时显示真实空态。

## 必做

1. 建立个人资产状态与本地资格判断；AI 不得改变阶段或资格。
2. 创建 `DailyPlan` 与有理由的 `TrainingTask`，同一输入快照产生确定结果。
3. 新人仅获得熟读、复现、单资产调用路径，禁止直接拼贴。
4. 今日训练页显示计划、理由、无资产/无资格空态。

## 禁止

- 不用 AI 生成资格或阶段；不伪造推荐；不提前解锁拼贴训练。
