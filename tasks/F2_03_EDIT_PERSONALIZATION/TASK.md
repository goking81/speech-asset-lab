# F2-03｜R2 编辑、R3 个人化与安全降级

## 目标

实现来源资产编辑草稿（R2）和个人化草稿（R3）的待确认预览；任何结果均不得覆盖来源/个人确认版本。

## 必做

1. R2 输出 changeSet 草稿，保留原来源版本与证据；用户确认后只能新建版本。
2. R3 只使用用户已确认的个人事实；事实缺失时安全停止并说明原因。
3. 节点与范围必须可追溯；不保存 UI 颜色。
4. Provider 失败或结构失败保留本地输入和任务 Attempt。

## 禁止

- 不编造个人经历；不自动发布；不覆盖确认版本；不创建训练计划。
