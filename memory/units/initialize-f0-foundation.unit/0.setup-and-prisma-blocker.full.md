---
id: 0.setup-and-prisma-blocker
unit: initialize-f0-foundation
title: F0 工程基础与 Prisma 阻断
brief: 0.setup-and-prisma-blocker.brief.md
source: 2026-07-24 F0 会话
updated: 2026-07-24
---

# F0 工程基础与 Prisma 阻断 — Full Memory

## Context

用户要求仅建立 F0 开发基础，不进入 Iteration 01 或任何业务功能。交接包在 `speech_asset_lab_codex_handoff_f0_20260723`，其中当前任务仍为 `tasks/ITERATION_01/TASK.md`，未被修改。

## Detailed notes

- 所有交接文件已读取；稳定逻辑文件与 `versioned_sources/` 的映射逐一通过 SHA-256 比对。
- 新增基础：`package.json`、pnpm lock/workspace 配置、TypeScript、ESLint flat config、Prettier、`.env.example`、`src/app` 最小启动页、`src/components|features|lib|server|ai|styles` 目录和 `public`。
- 环境模板使用 SQLite：`DATABASE_URL="file:../data/speech-asset-lab.db"`，无真实 Key；扫描未发现候选密钥。
- pnpm 11 构建脚本通过 `pnpm-workspace.yaml` 最小允许 Prisma 和 `unrs-resolver`，拒绝未使用的 `sharp`。
- 通过：`pnpm run format:check`、`pnpm run lint`、`pnpm run typecheck`、`pnpm run build`。Next.js 16.2.11 静态构建包含 `/` 与 `/_not-found`。
- 首次失败：`pnpm run prisma:format` 与 `pnpm run prisma:validate` 均为 P1012。根因是 schema 的 `enum InternalStage { S0 S1 ... }` 等 30 个单行 enum 不是 Prisma Schema Language 接受的 enum 值布局；官方 Prisma 文档示例为值逐行定义。
- 用户已确认语法等价修复后，30 个 enum 均改为逐行值定义。与版本化源比对后，30 个 enum 的名称和值顺序完全一致。
- 用户确认索引应使用现有 `startedAt` 后，`@@index([assetPracticeSessionId, stepType, createdAt])` 已改为 `@@index([assetPracticeSessionId, stepType, startedAt])`。
- 通过：`pnpm run prisma:format`、`pnpm run prisma:validate`、`pnpm run prisma:generate`、`pnpm run format:check`、`pnpm run lint`、`pnpm run typecheck`、`pnpm run build`。
- Prisma schema 与版本化交接副本的 Hash 已因上述受控修复而不同；30 个 enum 的名称和值顺序与原副本一致，去除 enum 格式和批准索引修复后的其余 schema 内容一致。

## Key excerpts / anchors

- `prisma/schema.prisma`：30 个 enum 为逐行定义；`AssetPracticeAttempt` 索引使用 `startedAt`。
- `docs/05_DATA_MODEL.md:14`：要求执行 Prisma format、validate、迁移和 seed。
- `docs/10_CONSISTENCY_AUDIT.md:34`：Prisma 实机验证是 F0 阻断检查。
- `tasks/CURRENT.md`：仍指向 Iteration 01，未推进。

## Implications

已完成的 enum 修复保持了 enum 名和值不变，且索引已按确认使用 `startedAt`。下一步是 F0 审核；审核通过并保持 `CURRENT` 指向 Iteration 01 后，才可开始应用壳与设置导航。不得提前进入 Iteration 02 或 F1。

## Open questions

- 是否应为受控 Prisma 修复创建新的不可变版本化 schema 副本并更新 Manifest？
- 当前包不含 Git 仓库，何时初始化版本控制由用户决定。
