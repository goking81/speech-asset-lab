# F0｜最终发布门禁（完整）

## 完成内容

F0 的前三轮已分别建立应用壳、SQLite/本地目录和 P05/P08/Mock AI 基础。最终审核发现 F0 任务仍要求最小 Checkpoint、日志和可重复发布门禁，已在不进入 F1 业务的前提下补齐。

- `SessionCheckpointService` 用既有 `SessionCheckpoint` Schema 保存会话恢复快照，并校验会话存在、类型非空和 payload 可 JSON 序列化。
- `appendLocalLog()` 使用 JSONL 写入本地日志目录，对 key 名含 apiKey、authorization、password、secret 或 token 的上下文脱敏。
- `verify-cold-start.mjs` 启动生产构建在 127.0.0.1:3101，确认首页成功响应后停止精确子进程。
- `verify-f0.mjs` 在 Windows 使用 shell 启动 pnpm，解决 `.cmd` 直接 spawn 的 EINVAL，并依次执行所有 F0 校验。

## 结果与后续

完整 F0 Gate 通过：Prisma、db setup、Prettier、ESLint、TypeScript、8 个 Vitest 文件/28 项、5 个 Playwright 用例、生产构建和冷启动均成功。发布门禁文档已从 F0 阻断更新为 F0 Pass。

F1 不是自动下一步：仍需负责人审核 F0 RESULT、将「来源与资产」路线图拆分为明确任务卡，并更新 Manifest 和 CURRENT。不得把 F0 的 Mock AI 误当成真实 Provider 能力。
