# F0｜最终发布门禁（简要）

- 新增 `SessionCheckpointService`：仅允许有效训练会话写入可 JSON 序列化的 Checkpoint。
- 新增 `appendLocalLog()`：写入本地 JSONL，凭据类字段一律脱敏。
- `pnpm run verify:f0` 统一执行 Prisma、SQLite 初始化、格式/lint/类型、Vitest、Playwright、生产构建与生产冷启动。
- 2026-07-25 门禁结果：8 个 Vitest 文件/28 项、5 个 Playwright 用例、冷启动均通过。
- F0 Gate 已通过；F1 仍须先有经审核任务卡并更新 Manifest/CURRENT。
