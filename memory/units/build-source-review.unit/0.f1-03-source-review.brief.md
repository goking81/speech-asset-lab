# F1-03｜候选审核与来源资产版本（简要）

- 审核页只展示真实本地数据：无解析段落时明确空态；用户可从完整来源段落手动创建候选。
- 候选必须有合法的段落 offset 证据；编辑只修改候选内容与节点，保留证据；确认、忽略均为单项操作。
- 确认在事务中建立 `SourceAsset`、不可变 `SourceAssetVersion`、节点和表达单元，并用 `CandidateAsset.sourceAssetVersionId` 保留一对一追溯。
- 新迁移 `20260725123000_candidate_asset_source_version_link` 已应用；SQLite 版本不可变触发器继续生效。
- 未启用 AI/R1/R2，不创建个人资产或训练计划。验证：38 项 Vitest、8 项 Playwright、build/lint/typecheck/Prisma 全部通过。
