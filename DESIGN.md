# Speech Asset Lab｜当前 UI、视觉规范与页面原型

版本：1.0  
导出日期：2026-08-08  
适用范围：Windows 本地运行的桌面优先 MVP

> 本文件记录当前已经落地的界面视觉与页面结构，供后续设计、开发与验收使用。它不改变产品规则；产品事实仍以 `tasks/CURRENT.md`、`docs/04_DECISIONS.md`、`docs/01_PRD.md` 和 `docs/02_PAGE_SPEC.md` 的优先级为准。

## 1. 导出文件清单

| 文件                                                     | 内容                                         | 状态         |
| -------------------------------------------------------- | -------------------------------------------- | ------------ |
| [`DESIGN.md`](DESIGN.md)                                 | 当前已实现的 UI 方向、视觉规范、页面原型总览 | 本次导出     |
| [`docs/02_PAGE_SPEC.md`](docs/02_PAGE_SPEC.md)           | MVP 页面职责、流程和验收标准                 | 当前规格     |
| [`docs/02_PROTOTYPE_SPEC.md`](docs/02_PROTOTYPE_SPEC.md) | 训练与设置的可点击原型状态规格               | 当前规格     |
| [`docs/03_DESIGN_SYSTEM.md`](docs/03_DESIGN_SYSTEM.md)   | 初始 Design System、节点颜色、表单与训练边界 | 当前规格基础 |
| [`docs/design-qa.md`](docs/design-qa.md)                 | 森林工作台界面的截图比对与验收记录           | 当前实现验收 |

历史冻结版本位于 [`versioned_sources`](versioned_sources)，用于追溯，不应覆盖当前实现。

## 2. 当前设计方向

**沉浸式森林学习台 / Warm Forest Learning Studio**。

界面由温暖的象牙白工作区、深森林绿侧栏和克制的自然系插图组成；目标是让长期英语表达训练呈现为安静、专注、可积累的个人工作台，而不是聊天产品、游戏化打卡产品或内容社区。

- 核心气质：温和、文艺、可信赖、长期学习。
- 视觉重心：真实资产、练习路径、来源证据和人工确认。
- AI 的视觉位置：可选的辅助与草稿，不占据主流程，不以机器人或对话气泡表现。
- 内容原则：所有标题、语流、资格状态、候选与来源证据都来自本地数据；不得用视觉原型中的虚构文本替换真实用户资产。

## 3. 视觉 Token

| 用途        | Token / 色值          | 使用方式                 |
| ----------- | --------------------- | ------------------------ |
| 页面底色    | `#FBF8F0`             | 柔和暖白画布             |
| 主森林绿    | `#294B2D`             | 侧栏、深色层级           |
| 主操作绿    | `#406343`             | 主按钮、已选状态         |
| 主操作悬停  | `#355737`             | 鼠标悬停                 |
| 卡片表面    | `#FFFDF8`             | 主卡、表单和内容容器     |
| 柔和浅绿    | `#EAF0E6`             | 标签、选中态、弱强调     |
| 分隔线      | `#E2DDD0`             | 卡片边框、页面分隔       |
| 主正文      | `#213528`             | 正文与重要信息           |
| 次级文字    | `#687365`             | 说明、元信息             |
| AI 辅助色   | `#8A694D` / `#F5EEE4` | 草稿、提示等次级区域     |
| 成功状态    | `#55775A`             | 本地保存、可训练等状态   |
| 警告 / 错误 | `#9B743A` / `#A34D45` | 只用于真实异常或风险反馈 |

卡片默认使用 15–18px 圆角、1px 暖灰边框和非常轻的阴影。页面不使用高饱和渐变、厚重阴影或大面积冷色背景。

### 排版

- 中文展示标题：`Georgia, Noto Serif SC, Songti SC, serif`，深绿、偏大字号；页面标题同时呈现中文与英文副标题。
- 英文副标题与长语流：`Arial, Microsoft YaHei UI, sans-serif` 或系统无衬线字体；保障连续英文可读性。
- 普通界面与表单：`Aptos, Segoe UI Variable, Microsoft YaHei UI, sans-serif`。
- 小型序号、状态和技术标签：`Cascadia Mono, Consolas, monospace`。
- 普通正文基准：14px / 22px；资产语流和阅读内容可提升至 16px / 26px 以上。

## 4. 全局应用壳

```text
┌──────────────────森林侧栏──────────────────┬────────────暖白工作区────────────┐
│ 品牌标识                                    │ 顶部：本地运行 / AI / 本地保存状态 │
│ 今日训练、素材库、资产库、学习笔记、设置…   │ 页面标题、内容卡、操作区           │
│                                             │                                    │
│ 底部森林插图与浅色模式入口                  │                                    │
└─────────────────────────────────────────────┴────────────────────────────────────┘
```

- 桌面优先。页面最小宽度为 960px；主体验以约 1486 × 1057 的桌面视口进行视觉验收。
- 左侧栏固定，采用森林背景、品牌常青树标记和浅色导航文字；当前项显示半透明浅绿高亮。
- 顶部状态条固定，显示“桌面优先 · 本地运行”、AI 服务状态和本地保存状态；状态必须反映真实配置，不能伪造“已配置”。
- 内容区为暖白画布，使用克制的细网格或低对比背景纹理；主要页面采用宽卡片与多列布局，避免内容仅占中央窄列。
- 详情与审核类页面在左上方保留可见的返回上一层入口；返回不是页面底部孤立按钮。

## 5. 页面原型总览

### 5.1 今日训练 `/`

```text
今日训练 / Today’s practice
说明文案

[训练卡 1（当前选择）] [训练卡 2] [训练卡 3]

┌────────当前资产详情────────────────────────────────────────────┐
│ 资产标题 + 主题标签                                             │
│ 语境 / Context              │ 核心表达 / Key flow             │
│ 五步训练路径：1 → 2 → 3 → 4 → 5                                │
│                  [开始五步训练]                                 │
└────────────────────────────────────────────────────────────────┘

[可折叠的 AI 补充（可选，草稿）]
```

- 顶部展示可选训练资产，每张卡配与资产主题相关的文艺自然小图。
- 当前选择资产具有绿色描边或完成标记；点击其他卡实时更新下方详情。
- 语境和核心表达并排；下方展示固定的五步路径。
- AI 说明只补充已有本地训练计划，不创建任务、不改变阶段或资格。

### 5.2 资产库 `/assets`

```text
我的语流资产 / My Assets                         [导入来源素材]
说明文案

[资产卡] [资产卡] [资产卡]
[资产卡] [资产卡] [资产卡]
```

- 使用三列平铺资产卡，减少长页面纵向滚动；窄屏时可退化为两列或单列。
- 单卡包含：主题图标、资产标题、类别标签、中文概述、英文核心语流摘要、来源/个人版本状态和进入详情入口。
- 可直接训练的资产显示叶片状态；仅有来源版本的资产显示只读与创建个人版本入口；导入的初始学习资产可以直接训练时，必须明确标注其可训练状态。
- 图标使用统一的 Phosphor 线性图标，按真实标题、核心观点和语流主题映射。例如：追问用对话图标、可信度用盾牌、目的用靶心、群体讨论用多人、饮水用水滴、旅行用山峰或飞机、晨间用太阳、学习用书本、自然用树叶。

### 5.3 资产详情与个人版本 `/assets/[id]`、`/assets/[id]/personalize`

- 页面左上方提供“返回资产库”。
- 来源版本始终以只读形式显示，并保留来源信息。
- 个人版本可独立编辑；个性化改写区可输入中文个人经历，AI 仅基于该经历和当前来源资产生成**待确认草稿**。
- 保存动作创建新的个人版本，不覆盖来源版本，也不绕过人工确认。

### 5.4 资产训练 `/train/assets/[id]`

```text
返回今日训练
资产训练 / Asset practice

步骤栏（1 熟读理解 · 2 复现 · 3 单资产调用 · 4 多资产拼贴 · 5 自由表达）
┌────────────────────────主要练习───────────────────────┐  ┌提示与来源┐
│ 语境 / Context                                          │  │按需展开  │
│ 核心表达 / Key flow，逻辑节点以文字颜色标识              │  │不自动干扰│
│                   [完成当前步骤]                        │  └──────────┘
└────────────────────────────────────────────────────────┘
```

- 保持连续自然段，不把语流拆成句子卡；逻辑节点颜色仅表示结构，不表示训练完成度。
- 第 2–4 步是口头练习与自报，不出现麦克风、波形、录音、发音评分或“系统正在听”。
- 多资产拼贴及自由表达受阶段门禁限制；新人不能跳过前序步骤。

### 5.5 候选审核 `/content/jobs/[jobId]`

```text
← 返回内容工作台
候选审核 / Review candidates                         [AI 筛选候选]

┌────────来源段落 / Source material──────┬────待确认候选 / Candidate drafts────┐
│ 选择文档                                │ 候选编号、标题、核心思想              │
│ 原文（只读）                            │ 关键表达（节选）                       │
│ 查看其他段落                            │ 来源证据 + [查看原文]                  │
│ 来源证据与说明                          │ [编辑] [确认资产] [忽略]               │
└────────────────────────────────────────┴─────────────────────────────────────┘

[折叠：手动建立候选草稿]
```

- 来源材料与候选审核卡固定为并排工作台；不展示已确认或已忽略的历史项，避免干扰当前审核。
- “AI 筛选候选”只请求草稿，系统会过滤不完整或无法支撑训练的内容；不为每个段落强行产出候选。
- 每条候选都必须展示来源证据，并支持在左侧定位原文。
- 只有用户点击“确认资产”后，才创建只读来源资产；不提供“一键全部确认”。

### 5.6 内容导入、问题训练、记录和设置

- **内容导入**：显示实际解析进度、解析结果、失败原因和下一步；PDF/OCR 结果仍需提供来源段落审阅，不能直接发布资产。
- **问题训练**：仅基于已掌握或可训练资产生成可回答问题；无可调用资产时展示真实空状态，不随机生成陌生问题。
- **训练记录**：以资产、问题、状态和历史结果为主，不伪造 AI 评价。
- **设置**：采用左侧页内导航和右侧表单卡；训练目标字段按等宽网格对齐，API Key 只显示掩码；未完成功能显示明确状态而不是无反应控件。

## 6. 图像、图标与品牌资产

| 路径                                         | 用途                             |
| -------------------------------------------- | -------------------------------- |
| `public/images/brand-evergreen-mark.png`     | 侧栏品牌常青树标记               |
| `public/images/forest-sidebar.png`           | 森林侧栏背景                     |
| `public/images/focus-leaf-badge.png`         | 可训练资产叶片标记               |
| `public/images/practice-forest-path.png`     | 自助、成长、自然类训练卡         |
| `public/images/practice-morning-desk.png`    | 晨间、专注、慢生活、阅读类训练卡 |
| `public/images/practice-mountain-walk.png`   | 旅行、经历、探索类训练卡         |
| `public/images/practice-swimming-relief.png` | 游泳、减压类训练卡               |
| `public/images/practice-hydration.png`       | 饮水、健康类训练卡               |
| `public/images/practice-conversation.png`    | 提问、沟通、对话类训练卡         |

插图必须与真实资产主题匹配，统一保持柔和、自然、低对比的文艺摄影感。资产库的语义图标优先使用 `@phosphor-icons/react`，不重复使用与主题无关的树或叶图标。

## 7. 交互、状态与可访问性

- 主按钮为森林绿，最小可点击高度约 40–52px；悬停可轻微上移，但不能让动画影响任务理解。
- 所有键盘焦点显示 2px 主色轮廓；链接、按钮和导航均需有清晰文本与可点击反馈。
- 禁用控件应说明原因；加载状态同时显示“正在保存 / 正在筛选 / 正在解析”等文字，不能只变灰。
- AI 输出统一标注草稿或可选辅助，不自动保存为正式资产，不覆盖用户内容。
- 中文与英文混排时，保持可读的行高和段间距；长英文语流采用自然换行、摘要截断和完整内容入口，不以压缩字体解决溢出。

## 8. 当前实现定位与验收

| 范畴                       | 主要实现位置                                     |
| -------------------------- | ------------------------------------------------ |
| 全站 Token、布局与页面视觉 | `src/styles/globals.css`                         |
| 应用壳与导航               | `src/components/app-shell.tsx`                   |
| 今日训练森林工作台         | `src/features/planning/today-plan-page.tsx`      |
| 资产库与主题图标           | `src/features/assets/asset-library-page.tsx`     |
| 候选审核双栏工作台         | `src/features/content/candidate-review-page.tsx` |
| 截图比对与验收结论         | `docs/design-qa.md`                              |

当前设计已通过本地 Chrome 桌面视口的今日训练、资产库、候选审核页面截图比对；详细参考图、最终截图路径与结论见 [`docs/design-qa.md`](docs/design-qa.md)。后续若调整视觉，应同时更新本文件、相应页面规格和 QA 记录。
