# GOAL.md

## 当前工程目标

完成 RC1 本地正式版发布收口：让 Windows 单用户能够在本机启动生产构建、在浏览器使用页面、保存本地数据，并通过可重复的自动化发布门禁。

## 当前阶段

RC1｜本地可运行交付与发布准备。

## 验收标准

1. `start-local.bat` 能初始化本地目录、迁移、seed、构建并启动应用；
2. `stop-local.bat` 只停止由本项目启动记录确认的本地服务；
3. SQLite、来源文件、日志、备份和隔离恢复目录均在本机受控目录内；
4. API 未配置时，本地功能可用且 AI 显示安全降级；
5. Prisma、类型、Lint、格式化、单元测试、E2E、生产构建、冷启动和迁移升级均可重复验证；
6. 真实 Provider 调用、语义审核、Bundle 批准/激活、Git Tag 和远程推送始终由用户人工决定。

## 固定范围

保持桌面优先、本地运行、单用户、SQLite、本地来源文件与外部 AI Provider Adapter。继续排除公网部署、登录、多用户、支付、社区、移动端、录音、音频、转写、发音评分和图谱全局可视化。
