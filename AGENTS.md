# AGENTS.md

## 项目使命

开发“英语语流资产工具”的桌面本地 MVP。核心单位是可追溯、可训练、可调用的**语流资产**，不是孤立句子。

## 事实来源与优先级

必须先读取 `docs/09_SOURCE_MANIFEST.md`。冲突时严格按以下顺序裁决：

```text
DECISIONS → 当前 TASK → PROMPT_COACH_RULES
→ PRD / PAGE_SPEC / PROTOTYPE_SPEC / DESIGN_SYSTEM
→ DATA_MODEL / ARCHITECTURE / ROADMAP
```

不得以旧文件、原型临时代码、Stitch 自动文案、历史聊天或个人推测覆盖当前定稿。

## 不可违反的产品原则

1. 来源资产和个人资产必须并存，来源可追溯，二者不可互相覆盖。
2. 新人先熟读、复现和单资产调用，达到门槛后再解锁多资产拼贴。
3. AI 问题、骨架和追问必须由用户当前已批准、可调用的资产支撑。
4. AI 输出均为草稿或语义判断；正式资产、关系、阶段和总分由用户或本地规则确认。
5. 用户输入先事务性保存，再启动 AI；AI 失败不得造成输入丢失。
6. 纯文字 MVP 不采集、推断或评价录音、发音、停顿、语速或真实口语流利度。
7. 本地规则拥有阶段、资格、门禁、总分、幂等和正式状态更新的最终裁决权。

## 固定范围

- 桌面优先；最低保证 1024px 可用，1280px 为推荐工作宽度；
- 本地运行、localhost、单用户；
- SQLite + Prisma；
- 来源文件和训练记录保存在本地；
- AI 通过 Provider Adapter 调用外部云端 API；
- 不做移动端、登录、多用户、支付、社区、实时语音、录音、音频转写、发音评分和正式公网部署。

## P05 强制实现规则

1. 第 1 步原句使用连续自然段；逻辑节点只通过不同文字颜色和可访问语义标记区分，不显示数字、编号块或逐句分段。
2. 第 2—4 步为口头练习，系统不录音、不调用麦克风、不转写；只记录用户自报结果。
3. 第 3 步显示中文逻辑骨架；第 4 步不显示关键词、骨架、英文词伙或完整语流。
4. 第 5 步才出现文字输入；只分析用户实际提交的文字。
5. 口头步骤的“保存并继续”必须同时满足：已确认完成口头尝试、已选择完成情况、已选择难度自评、当前不在保存中。
6. 第 5 步文字提交条件：`trimmedText.length > 0`、会话有效、未在提交中。禁止隐藏词数或字符数门槛。

## P08 强制实现规则

- 第一次回答、追问回答、第二次回答只要去除首尾空白后非空即可提交；
- 不得使用最少词数、最少句数或“内容太短”作为提交阻断；
- AI 可在提交后返回 `INSUFFICIENT_TEXT` 等评价状态，但这属于评价结果，不是输入门禁；
- 提交时先保存 `TrainingAnswer`，再创建 AI Task。

## 设置页强制实现规则

设置页左侧或页内导航必须是真实可点击的锚点、Tab 或子路由。每个入口必须满足其一：

- 展示可操作设置内容；
- 展示明确的“当前版本暂无可配置项”空状态，并说明是否属于 P1。

不得保留点击无反应的视觉导航。

## 工程行为

- 每次只执行 `tasks/CURRENT.md` 指向的一个任务；
- 不擅自修改范围、术语、训练顺序或解锁规则；
- 每个任务必须包含代码、测试、错误状态、迁移或数据影响、验收记录；
- 任何新决策先写入对应文档并更新 Manifest，再实现；
- 不用 mock 成功掩盖未实现功能；明确显示空状态、失败状态或“尚未实现”；
- 禁止将 API Key 输出到浏览器、日志、测试快照或错误消息；
- 禁止在正式字段中保存原始颜色作为业务语义；逻辑节点颜色由 `NodeType` 和设计 Token 派生。

## 任务完成输出

每个 Iteration 结束后在仓库根目录输出 `RESULT.md`，至少包含：

- 实现摘要；
- 修改文件；
- 数据迁移；
- 自动测试结果；
- 手工验收结果；
- 与原型或文档的偏差；
- 未解决问题；
- 是否建议进入下一 Iteration。
